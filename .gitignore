node_modules/
# Build output (toba's own dist only — anchored so the vendored, prebuilt
# vendor/velum-ai/dist is still committed and travels with a fresh clone).
/dist/
state/
.claude/
.agents/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
!.env.example
.DS_Store
*.log
.vitest-cache/
coverage/
*.tsbuildinfo
